# Playbook — pratiques qui marchent

[2026-07-20] | Recherche SEO DataForSEO à petit budget : 1 appel search_volume (35 seeds) + keyword_suggestions + related_keywords + 5 SERP live + ranked_keywords du leader = 0,17 $ | Vision complète marché + concurrents pour moins de 20 centimes
[2026-07-20] | Pattern d'appel API : payload JSON dans un fichier scratchpad + curl -u "$(cat creds.txt)" + petit node -e pour extraire | Traçable (JSON bruts conservés), rejouable, identifiants jamais dans le projet
[2026-07-20] | Analyser les ranked_keywords du concurrent n°1 (lesvertsmoutons.com) plutôt que deviner | A révélé le modèle gagnant : 1 fiche produit forte + 4 guides blog qui verrouillent les requêtes confiance/usage
[2026-07-20] | Pages guide Astro : contenu (FAQ, tableaux, étapes) en tableaux d'objets dans le frontmatter + composants partagés Faq/CtaPacks + helpers lib/seo.ts | Fichiers < 300 lignes, schema JSON-LD généré depuis la même source que l'affichage (zéro divergence), vérifiable par grep sur dist/
[2026-07-20] | Avant de rédiger des mentions légales/CGV, vérifier via WebFetch si un site déjà en ligne pour la marque existe (ex. aimezlanature.fr) plutôt que d'inventer ou de mettre un placeholder | Récupère les vraies infos légales (raison sociale, SIRET, hébergeur) sans halluciner ni bloquer sur une question à l'utilisateur
[2026-07-20] | Récupérer les assets (photos produit, logo) depuis le site en ligne : `curl` du HTML brut puis grep sur `data-large_image`/`wp-post-image` — WebFetch ne voit que les placeholders lazy-load | Vraies photos du client, aucune image inventée ; le sitemap produit du site en ligne révèle aussi le catalogue réel (a permis de détecter que le Pack 35 n'existait plus)
[2026-07-20] | Rendre optionnels les champs d'asset dans les schemas (`buildProductSchema(p, imageUrl?)`) + une map slug→image dans un seul module | Un produit sans photo dégrade proprement (visuel de substitution, champ omis) au lieu d'émettre une URL 404 ; ajouter une photo = une ligne à changer
[2026-07-20] | Sur un site statique avec l'adaptateur Cloudflare, forcer `cloudflare({ imageService: 'compile' })` | Les WebP sont générés au build par sharp et servis en statique avec cache immuable, au lieu de passer par Cloudflare Images (facturé) à chaque affichage
[2026-07-20] | Avant de reprendre une fiche produit du site existant, vérifier que la note affichée appartient bien au produit et pas à un bloc de cross-sell (ici 4,94 et 4,91 étaient les packs suggérés, la gourde n'avait aucun avis) | Évite de publier une note inventée — interdit par Google et trompeur ; rendre `rating`/`reviewCount` optionnels résout le cas proprement
[2026-07-20] | Ne pas recopier les allégations marketing du site existant (« 2,3x mieux qu'un filtre », « dure 10 ans ») sans source | Le site assume une ligne honnête (page À propos, guide Avis) : une allégation non sourcée la contredit et expose côté allégations commerciales
[2026-07-20] | Récupérer les VRAIS avis clients depuis le JSON-LD WooCommerce du site en ligne (curl + regex sur "reviewBody"/"name") | Preuve sociale authentique et défendable (noms + textes exacts), zéro invention
[2026-07-20] | Contraste : calculer les ratios WCAG en node (petite fonction luminance) pour choisir une teinte de remplacement AVANT d'éditer, puis sed sur toutes les occurrences | Une seule passe, 44 occurrences corrigées d'un coup, Lighthouse A11y 95→100
[2026-07-20] | CSP stricte sur site statique Astro/Cloudflare : `inlineStylesheets: 'never'` + JS dans public/js/*.js + polices @fontsource auto-hébergées → `default-src 'self'` sans unsafe-inline (les JSON-LD inline ne sont pas concernés par la CSP) | Sécurité maximale sans rien casser, vérifiable en grepant dist pour <style et <script inline
[2026-07-20] | Animations légères sans lib : [data-reveal] + IntersectionObserver + CSS sous `@media (scripting: enabled) and (prefers-reduced-motion: no-preference)` | Contenu visible sans JS, respect reduced-motion, CLS 0 mesuré, zéro dépendance
[2026-07-20] | Section blog : articles neufs sur les requêtes NON couvertes par les piliers (trous du mapping DataForSEO) plutôt que recréer les anciens posts déjà 301-és | Zéro cannibalisation ; vérifiable en contrôlant l'unicité des questions FAQPage sur tout le dist (script node, 70 questions, 0 doublon)
[2026-07-23] | Vérifier une feature JS interactive quand l'onglet d'automatisation est instable : `requestAnimationFrame` et IntersectionObserver ne se déclenchent pas dans un onglet en arrière-plan (non peint), et un `scrollTo` programmatique affiche une capture blanche. Contourner en (1) lisant l'état du DOM directement (`classList`, `.hidden`, `localStorage`) qui, lui, est fiable ; (2) utilisant le VRAI scroll via le tool `computer` (roulette) + capture, qui force le rendu | Preuve terrain d'un comportement au défilement/clic sans se fier à des API tributaires du paint. A permis de confirmer barre collante + ajout panier + synchro prix
[2026-07-23] | Barre d'achat collante sans lib ni dépendance de prix dupliquée : bouton `data-ajouter-panier` intercepté par délégation dans cart.js (même chemin que le BuyBox), total recopié depuis `[data-palier-total]` du palier coché | Zéro divergence de prix (33,84 € identique barre/panier vérifié), une seule source de vérité pour le calcul
[2026-07-23] | Skills design ui-ux-pro-max (recherche best-practices, base locale) puis impeccable (critique/audit) : traiter la sortie de la base comme INSPIRATION, pas comme vérité — elle a proposé une DA bleu « Liquid Glass » qui aurait cassé la marque naturelle existante. Garder le pattern transposable (Before-After), jeter le reste | Recommandations fondées + marque préservée ; le pattern retenu vient des données, la DA reste celle du client
[2026-07-23] | Nouveau bloc de vente sur fond coloré : mesurer le contraste WCAG de CHAQUE texte au navigateur via un resolver canvas (gère oklch/oklab/color() de Tailwind v4 que la regex ne lit pas) AVANT de considérer le travail fini | Le texte « avant » gris-sauge à 70 % passait de justesse (5,30:1) — sans mesure j'aurais pu shipper un échec a11y qui contredit la rigueur des audits précédents
[2026-07-23] | Avant de construire une page « métier » (offre revendeur B2B), vérifier la demande réelle avec DataForSEO AVANT d'écrire quoi que ce soit : « grossiste perles de céramique », « perles de céramique en gros », « revendeur produits écologiques » = 0 recherche/mois en France | A évité de bâtir un cluster SEO inutile. La page a été conçue comme un support de démarchage (une seule page, lien discret en pied de page, hors menu principal) au lieu d'une machine à trafic — pour 0,05 $ de requête
[2026-07-23] | Formulaire public sur site à CSP stricte : `form-action 'self'` interdit Tally/Google Forms/Formspree, et un captcha tiers (Turnstile) obligerait à relâcher `script-src 'self'`. Solution : `<form method="post">` natif vers une route Astro `prerender = false` sur le Worker, anti-robot par champ piège + horodatage, redirection 303 vers une page de remerciement | Formulaire fonctionnel SANS JavaScript (prouvé : POST curl → 303 vers /revendeurs/merci/), zéro script tiers, CSP intacte. Le JS ne fait qu'améliorer (les champs saisis survivent à une erreur)
[2026-07-23] | Données non validées par le client : structurer la page pour l'absence de donnée plutôt que d'attendre. `paliersRemise: PalierRemise[] = []` dans le fichier de données + un ternaire dans la page (tableau si rempli, texte de principe si vide) | La page est livrable aujourd'hui sans inventer un seul chiffre ; le jour où le marchand valide sa grille, il suffit de remplir le tableau — aucun fichier de présentation à retoucher. Vérifié par un script qui compte les pourcentages dans dist/
[2026-07-23] | Reprendre les IDÉES d'une présentation/maquette externe sans en copier le CONTENANT : d'une présentation interactive à défilement (incompatible CSP + mauvais pour le SEO car contenu JS-dépendant), extrait 3 patterns transposables — rituel en 3 verbes, « nommer le problème avant la solution », e-book en argument d'achat — portés à la main en .astro statique. Chaque idée passée au crible « demande réelle ? » (DataForSEO : 0/mois → conversion, pas SEO) et « déjà sur le site ? » (grep avant d'écrire) | Le site gagne la valeur narrative de la présentation sans sa dette technique ; zéro doublon (le bloc avant/après existant n'a pas été redondé), zéro chiffre inventé. Vérifié au navigateur (contrastes 7-17:1, 0 débordement 390px, PDF servi en application/pdf)
[2026-07-28] | Collecte d'avis clients sans base de données ni service tiers : `<form method="post">` natif → route `prerender = false` → vérification de l'achat chez Stripe → email au marchand → publication manuelle dans reviews.ts. La route renvoie en plus la ligne `reviews.ts` PRÊTE À COLLER, générée avec `JSON.stringify` (échappement des apostrophes/retours ligne gratuit) | Zéro donnée personnelle stockée, zéro coût, zéro surface d'attaque, modération par construction, CSP intacte. Le marchand n'a qu'un copier-coller à faire, sans risque de casser le fichier
[2026-07-28] | Vérification d'une condition qui fonde une affirmation publique (badge « Achat vérifié ») : coder en FAIL-CLOSED explicite — clé absente, API injoignable ou réponse inattendue renvoient `null`, traité comme un REFUS, jamais comme un accord tacite. Prouvé au runtime avec une clé Stripe bidon (401 → 503, jamais 200) | Une panne d'API ne peut pas transformer un avis non vérifié en avis « vérifié ». C'est la seule façon de tenir la promesse du badge sans dépendre de la chance
[2026-07-28] | Deux formulaires publics = deux occasions de diverger. Piège anti-robot et horodatage extraits dans `src/lib/antispam.ts`, `data/revendeur.ts` les RÉ-EXPORTE au lieu de les redéclarer | Source unique de vérité sans toucher au code du formulaire revendeur qui marchait : vérifié dans `dist/` que `name="site_web"` et `name="ouvert_a"` sont toujours là (aucune régression), et le prochain formulaire héritera des mêmes filtres
[2026-07-28] | Avant de coder une intégration API sur un paramètre dont je n'étais pas certain (`customer_details[email]` du endpoint List Checkout Sessions), vérification dans la doc Stripe officielle par WebFetch | Confirmé le paramètre ET découvert que `status=complete` ne garantit PAS le paiement (« payment processing may still be in progress ») → le contrôle supplémentaire `payment_status === 'paid'` était indispensable. Sans cette lecture, un panier en paiement différé aurait validé un « Achat vérifié »
[2026-07-29] | Avant de toucher aux DNS d'un domaine en production, RELEVER l'état complet (A, MX, TXT, DMARC) via `Resolve-DnsName -Server 8.8.8.8` et le publier dans la conversation comme point de restauration. Puis vérifier après coup que le A du site et les MX de la boîte mail répondent toujours | Filet de sécurité gratuit : le rollback est un copier-coller. La relecture après modification a prouvé en 10 secondes que le site (49.12.107.224) et Zoho (mx.zoho.eu) étaient intacts — affirmation vérifiée, pas supposée
[2026-07-29] | Vérifier un domaine d'envoi sur un domaine qui a DÉJÀ des emails en production : préférer un fournisseur dont les enregistrements vivent sur un SOUS-DOMAINE (`emailit.`, `emailit._domainkey.`). Regarder les valeurs `name` renvoyées par l'API AVANT de conclure au risque | Emailit demandait `aimezlanature.fr` comme domaine mais toutes ses lignes sur `emailit.*` : risque réellement nul, et l'adresse d'expéditeur reste `@aimezlanature.fr` (jolie). J'allais créer un sous-domaine `send.` inutile — qui était EN PLUS déjà pris par Resend. La lecture des données a évité une collision
[2026-07-29] | Automatisation d'un formulaire web lourd (Namecheap) : cibler les éléments par `ref` via `find`, jamais par coordonnées. La page défile entre le screenshot et le clic, et les coordonnées dérivent systématiquement | 4 clics perdus par coordonnées, 0 par ref. Pour les listes déroulantes personnalisées, `form_input` sur le combobox marche là où le clic sur l'option échoue silencieusement
[2026-07-29] | Quand un même déclencheur doit envoyer PLUSIEURS emails et que l'émetteur (Stripe) rejoue en cas d'erreur : ordonner les envois par importance décroissante du destinataire, et n'autoriser le rejeu que sur le DERNIER. Ici marchand d'abord (échec seulement journalisé), client en dernier (échec → 500) | Une panne sur la notification interne ne peut pas provoquer une double confirmation chez l'acheteur. L'asymétrie est volontaire et documentée dans le code : un doublon dans la boîte du marchand est moins grave qu'un doublon chez le client
[2026-07-30] | Bandeau de consentement sur site à CSP stricte, sans CMP tierce : script externe dans `public/js/` (l'inline est interdit), Consent Mode v2 en `denied` posé AVANT tout, gtag.js injecté seulement au clic « Accepter », bandeau en `position: fixed` et `hidden` par défaut, identifiant passé par `data-` depuis un fichier de données Astro | Zéro service tiers, zéro coût, une seule ouverture de CSP au lieu de deux (Cookiebot en aurait exigé une seconde). Vérifié en production : 0 script Google avant choix, CLS toujours 0,00 avec bandeau affiché, aucune violation CSP. `hidden` par défaut donne le fail-closed gratuitement : sans JavaScript rien ne se charge, donc il n'y a rien à consentir
[2026-07-30] | Ne pas deviner la liste des domaines à autoriser dans une CSP pour un tiers : lire la doc officielle du fournisseur (ici developers.google.com/tag-platform/security/guides/csp), PUIS relire les requêtes réellement émises au navigateur pour confronter les deux | Les 7 domaines réellement contactés étaient tous dans la liste documentée — et cette relecture a confirmé l'ajout de `www.google.fr`, que la doc ne mentionne que sous la forme `www.google.<TLD>` et qu'un copier-coller aurait oublié. Une CSP incomplète échoue en silence
[2026-07-30] | Avant de « rate-limiter /api/* », inventorier les protections DÉJÀ en place route par route (`grep -nE 'antispam|piege|signature' src/pages/api/*.ts`). Ici 3 des 4 routes étaient déjà couvertes (piège + délai, ou signature HMAC) : le trou réel était UNE route, `/api/checkout` | Le périmètre a fondu de 4 routes à 1, et surtout l'inventaire a fait ressortir que `/api/stripe-webhook` ne devait SURTOUT pas être limité — Stripe compte tout non-2xx comme un échec de livraison. Un rate limit posé « sur /api/* » aurait cassé les emails de confirmation
[2026-07-30] | Limite anti-abus sans dépendance ni magasin externe sur Vercel : compteur en mémoire (Fluid Compute réutilise les instances chaudes), clé = `x-forwarded-for` que Vercel réécrit et ne laisse pas falsifier, fail-open partout, compteurs cloisonnés PAR ROUTE, et mémoire bornée pour que la protection ne devienne pas elle-même le déni de service | Zéro dépendance, indépendant du plan Vercel — donc pas rejouable l'échec du binding Cloudflare payant. Vérifié en production : 10 appels passent, le 11e renvoie 429 + `Retry-After: 30`, et 20 POST sur le webhook ne déclenchent AUCUN 429. Le cloisonnement est le test qui compte : un flot sur le formulaire revendeur laisse le paiement ouvert pour la même IP
[2026-07-30] | Corriger un CLS causé par les polices sur un site à CSP stricte : préchargement `<link rel="preload" as="font" crossorigin>` des SEULES polices de la ligne de flottaison, avec l'URL obtenue par un import Vite `?url` (jamais un chemin en dur, l'empreinte change à chaque build) + sous-ensembles `@fontsource/*/latin-*.css` au lieu des fichiers complets | CLS 0,11 → 0,00 mesuré (accueil 2 fois + fiche produit, l'insight CLSCulprits disparaît de la trace), et 36 → 6 fichiers woff2 déployés (540 → 144 Ko). `crossorigin` est obligatoire même en même origine, sinon le fichier est téléchargé deux fois et le preload ne sert à rien. Ne pas précharger toutes les graisses : ça vole de la bande passante à l'image LCP
[2026-07-30] | Vérifier qu'une police est VRAIMENT appliquée et pas silencieusement remplacée par le repli : `await document.fonts.ready` puis `document.fonts.check('600 16px "Playfair Display"')` et `getComputedStyle(h1).fontFamily` au navigateur | Un build qui passe ne prouve pas qu'une police s'affiche. A confirmé les 6 polices `loaded` et l'usage réel de Playfair/Inter après avoir changé les imports CSS — le seul contrôle qui distingue « déclarée » de « utilisée »
[2026-07-30] | Tester une config de routage (`vercel.json`) sans déployer : un petit serveur node qui rejoue la sémantique de la plateforme CONSTATÉE sur le déploiement réel (ici : `(.*)` traverse les slashs — prouvé via la règle `headers: "/(.*)"` appliquée aux chemins imbriqués ; `:path*` s'arrête au slash — prouvé par 308 sans slash / 404 avec), puis lancer `check-redirects.mjs` contre ce serveur. Rejouer le tout en INVERSANT l'ordre des étapes de routage | 220 requêtes validées en local, et le double passage prouve que le résultat ne dépend pas d'un ordre que Vercel ne documente pas. N'encoder dans le mock que des comportements mesurés, jamais supposés — sinon on ne teste que ses propres hypothèses
[2026-07-30] | Prouver qu'un correctif de TEST fonctionne vraiment : rejouer l'ANCIENNE version du test contre la panne reproduite. `git show HEAD:scripts/x.mjs > dossier-isole/scripts/x.mjs` + une copie du fichier de config à côté (le script résout ses chemins en relatif) | Résultat sans appel : l'ancien script affichait « 60/60 OK, sortie 0 » sur la panne exacte que le nouveau détecte (« 118/220, sortie 1 »). Sans cette contre-épreuve, « j'ai corrigé le test » reste une affirmation ; avec elle, c'est une démonstration
[2026-07-29] | Audit de couverture des redirections avant une bascule DNS : partir du sitemap RÉEL de l'ancien site (`wp-sitemap.xml` + ses 5 sous-fichiers = 67 URLs), puis un script qui classe chaque URL en 3 catégories — identique / couverte par une règle / NON couverte — en compilant les sources `vercel.json` en RegExp et en testant l'existence de la cible dans `dist/`. Le même script détecte les chaînes (une destination qui est aussi une source), les destinations mortes et les 302 | A trouvé 13 URLs non couvertes dont `/perles-de-ceramique-em/`, la page en correspondance exacte avec le mot-clé principal, que personne n'avait vue. Exhaustif et rejouable, là où une relecture à l'œil du fichier de redirections rate toujours quelque chose
[2026-07-29] | Pour auditer un déploiement Vercel protégé par SSO, `get_access_to_vercel_url` (MCP Vercel) rend un lien à jeton, puis `curl -c cookiejar -L` sur ce lien et `-b cookiejar` sur toutes les requêtes suivantes | Permet de tester les VRAIS codes HTTP (308/404) de la plateforme — les redirections `vercel.json` ne sont pas rejouables en local, et c'est exactement là qu'était le bug du joker `:path*`
[2026-07-29] | Ne pas croire une note de perf antérieure sur parole : `SEO-NOTES.md` annonçait CLS 0,00, la trace Chrome DevTools réelle donne 0,11 avec la cause racine nommée (swap des 6 polices à 396 ms). Mesurer avec `performance_start_trace` puis `performance_analyze_insight` sur `CLSCulprits` | La cause racine sort en une commande, ce qui transforme « le CLS est un peu haut » en « précharger inter-latin-400 et playfair-700 ». Un chiffre de perf a une date de péremption : il vaut pour le code du jour où il a été pris
[2026-07-29] | Juste AVANT une suppression sur un système en production, relire l'état réel plutôt que se fier au relevé fait plus tôt dans la session. Ici la ligne SPF `enginemailer` avait disparu entre le diagnostic du matin et le nettoyage du soir : je m'apprêtais à chercher — voire supprimer — une ligne qui n'existait plus | Une suppression basée sur un état périmé, c'est au mieux du temps perdu, au pire la mauvaise ligne effacée. Vérifier après coup sur le serveur AUTORITAIRE (`-Server pdns1.registrar-servers.com`) et non sur un résolveur public : le cache masque le résultat pendant la durée du TTL et laisse croire à un échec
