# Lessons — anti-patterns à éviter

[2026-07-20] | L'API DataForSEO renvoyait 40104 « verify your account » alors que l'email était confirmé | Un compte DataForSEO neuf exige email + SMS téléphone ; après validation, compter ~1 min de propagation et retenter (des 40104 sporadiques peuvent persister quelques minutes)
[2026-07-20] | `fetch` de Node échouait vers api.dataforseo.com alors que curl passait | Sur cette machine, faire les appels HTTP externes avec curl (Bash), pas avec fetch Node
[2026-07-20] | `npm run build` échouait en EPERM sur `dist/client` : des shells/node lancés en arrière-plan depuis `dist/client` gardaient le dossier verrouillé (Windows) | Lancer un serveur de preview depuis la racine du projet, et l'arrêter via PowerShell (`Get-Process python \| Stop-Process`) plutôt qu'en backgroundant `( ... & )` depuis un sous-dossier
[2026-07-20] | Le schema Product pointait vers `/images/{slug}.jpg` et og:image vers `og-default.jpg` — deux fichiers qui n'ont jamais existé | Après avoir écrit un chemin d'asset en dur, vérifier qu'il existe vraiment (`ls public/...` ou grep sur `dist/`) : un build qui passe ne prouve pas qu'une image référencée existe
[2026-07-20] | Le site affichait « 192 avis · 4,92/5 » à 7 endroits + dans le schema Organization, alors que le vrai total est 96 (45 + 51) — le 192 (= 96 × 2) ne venait d'aucune source. C'est l'utilisateur qui l'a repéré, pas moi | Tout chiffre de preuve sociale (avis, notes, nombre de clients) doit être recompté à la source AVANT d'être affiché ou envoyé à Google : en France un avis non vérifiable = pratique commerciale trompeuse (L121-2). Ne jamais laisser passer une donnée héritée sans la vérifier sous prétexte qu'on ne l'a pas écrite soi-même
[2026-07-20] | Ces chiffres étaient dupliqués en dur dans index.astro au lieu d'utiliser `SITE.*`, d'où la divergence invisible | Centraliser toute donnée affichée à plusieurs endroits dans une constante unique, et commenter la source + la date de vérification à côté de la valeur
[2026-07-20] | Le schema Product et l'og:image pointaient vers `photo.src` (ImageMetadata importée) : Astro n'émet PAS le fichier original dans dist quand l'image n'est utilisée que via <Image> (qui ne génère que des .webp) → URL 404 envoyée à Google sur les 4 fiches | Pour une URL d'image destinée au schema/og, toujours passer par `getImage({ src, format, width })` qui émet réellement le fichier, puis vérifier `ls dist/` que l'URL résout
[2026-07-20] | Les 3 témoignages nominatifs de l'accueil (« Marie L. »…) étaient inventés — même famille de bug que les « 192 avis » : de la preuve sociale fabriquée passée inaperçue | Tout témoignage affiché doit être un extrait mot pour mot d'un avis vérifiable à la source (ici : JSON-LD des fiches du site en ligne) avec un commentaire dans le code indiquant la source et la date de relevé
[2026-07-20] | Trois formulations différentes du don 2,5 % (« bénéfices » / « chaque commande » / « votre achat ») coexistaient sur le site — et l'engagement n'existe nulle part sur le site en ligne actuel | Une promesse chiffrée = une constante unique dans SITE.* + vérifier qu'elle a une source réelle avant de l'afficher ; à défaut, choisir la formulation la moins engageante et la faire confirmer
[2026-07-20] | Scripts inline `node -e`/`python -c` : Bash mange les backslashes des regex → erreurs de parsing en boucle | Dès qu'un script contient une regex ou des échappements, l'écrire dans un fichier du scratchpad et l'exécuter, ne pas le passer en argument inline
[2026-07-20] | Le « don de 2,5 % pour des puits » affiché sur tout le site avait été inventé lors d'une session précédente — et lors de l'audit je l'ai seulement « unifié et signalé » au lieu de le supprimer ; c'est l'utilisateur qui a dû insister | Un engagement (don, certification, partenariat) sans source vérifiable ne se reformule pas et ne s'affiche pas « en attendant confirmation » : il se RETIRE immédiatement, et ne revient que si le marchand le confirme par écrit
[2026-07-23] | La barre d'achat collante ne montait JAMAIS : un IntersectionObserver posé sur `#repere-achat`, un `<div>` vide de **hauteur 0**, ne délivre aucun callback (même pas l'initial) — une cible sans surface n'est pas observable de façon fiable | Ne jamais observer un élément de hauteur/surface nulle avec IntersectionObserver. Soit lui donner une hauteur réelle, soit — plus robuste — remplacer par un handler `scroll` throttlé en `requestAnimationFrame` qui lit `getBoundingClientRect().top`. Et toujours VÉRIFIER un déclencheur au défilement au navigateur réel, pas seulement au build
[2026-07-23] | La passe de contraste `#75592F → #6E5330` avait été faite dans tous les `.astro`/`.css` mais avait raté 2 occurrences codées en dur dans des chaînes de gabarit JS de `public/js/cart.js` (lignes de panier générées au clic) — Tailwind les compilait encore, donc l'ancienne teinte survivait dans le tiroir | Pour un remplacement global d'une valeur (couleur, texte, classe), grep sur TOUS les types de fichiers y compris les chaînes JS (`public/js/*.js`), pas seulement les composants. Vérifier ensuite qu'il ne reste 0 occurrence dans `dist/` (RÈGLE N°1)
[2026-07-23] | J'ai posé `[data-reveal]` sur les 4 cartes d'arguments ET les 4 étapes de la page revendeur, alors que `src/styles/global.css:60` porte l'avertissement explicite « Ne jamais poser [data-reveal] sur un bloc indispensable à la vente : tant que site.js n'a pas tourné, l'élément est à opacity 0 ». Les deux blocs sont apparus VIDES sur la capture pleine page | Avant d'utiliser un utilitaire maison (attribut, classe, helper), lire le commentaire qui l'accompagne dans le CSS/JS : ce projet documente ses pièges à l'endroit exact où on est tenté de les reproduire. Et toujours faire une capture PLEINE PAGE d'une nouvelle page : c'est ce qui a révélé les blocs invisibles, un snapshot d'accessibilité les aurait montrés « présents »
[2026-07-23] | Trois textes rendus avec un mot collé au lien suivant (« Écrivez-nous àcontact@… », « voir nosmentions légales », « et816 commentaires ») : Astro/JSX supprime l'espace de fin de ligne qui précède une balise ou une expression `{...}` | Quand une phrase se poursuit par `<a>`, `<em>` ou `{expression}` sur la ligne suivante, écrire explicitement `{' '}` en fin de ligne. Vérifier ensuite dans `dist/` avec une regex `[A-Za-zÀ-ÿ]{2,}<(a |em>|strong>)` — le scan a d'ailleurs révélé 6 occurrences PRÉEXISTANTES du même défaut sur d'autres pages
[2026-07-23] | Mes tests curl de la route POST ont tous renvoyé « 403 Cross-site POST form submissions are forbidden » et j'ai failli conclure à un bug de mon code | Astro protège les routes à la demande par un contrôle d'origine (CSRF) : un POST sans en-tête `Origin` est rejeté. Un vrai navigateur l'envoie toujours, curl non → ajouter `-H "Origin: http://localhost:PORT"` pour tester une route POST, sinon on teste la protection, pas sa logique
[2026-07-23] | J'ai qualifié de « malhonnête / pseudo-science » l'e-book officiel du fabricant fourni par le marchand, AVANT de savoir qu'il est revendeur agréé EM avec la documentation scientifique du procédé Higa. C'était un excès : la terminologie EM (« antioxydant », « revitalisation », « structure de l'eau ») est celle du fabricant qu'il distribue officiellement — reprendre le vocabulaire d'une marque dont on est revendeur agréé n'est pas de la fabrication. Le marchand l'a mal vécu, à juste titre | Distinguer AUTHENTICITÉ (le produit/la marque sont-ils réels ? → ici oui, vérifiable) de CONFORMITÉ D'UNE FORMULATION CHIFFRÉE précise (« 120 x moins coûteuse », « 10 ans » → doivent juste pouvoir s'appuyer sur une source si un client le demande). Ne pas écraser cette nuance en un jugement global « c'est malhonnête ». Poser la question du sourcing d'un chiffre AVEC RESPECT et sans présumer la mauvaise foi, surtout sans avoir vu les pièces. Lire quand même tout document avant publication (il engage le marchand), mais formuler une éventuelle réserve comme un point de conformité étroit, pas comme une accusation
[2026-07-23] | `astro dev` a refusé de démarrer plusieurs fois de suite (« Dev server process exited before becoming ready » / « failed to start within 30s »), et `astro preview stop` / `dev stop` NE stoppent pas mais LANCENT un nouveau serveur sur le port par défaut | Sur cette machine : pour vérifier au navigateur, utiliser `astro preview` (sur le build) en tâche de fond plutôt que `astro dev`, l'arrêter via TaskStop (id de la tâche) ou en tuant le process node en PowerShell (`Get-CimInstance Win32_Process ... Stop-Process`). Il n'existe pas de sous-commande `stop` fiable
[2026-07-28] | J'ai REFAIT le défaut d'espacement déjà consigné le 2026-07-23 (« sur cette page.* » collé) en écrivant AvisForm.astro, alors que la leçon existait et que j'avais lu le rappel dans merci.astro. Il n'est apparu que sur la capture d'écran | Une leçon écrite ne suffit pas si rien ne la fait respecter. Pour tout nouveau composant `.astro` : après le build, passer le scan `grep -oE '[A-Za-zÀ-ÿ.]{2,}<(a |em>|strong>|span)' dist/client/**/index.html` AVANT de dire que c'est fini. Le contrôle coûte 5 secondes, la relecture humaine rate le défaut une fois sur deux
[2026-07-28] | Pour ouvrir le formulaire d'avis au navigateur, `document.querySelector('details.group')` a ciblé le MAUVAIS élément : `ProductReviews.astro` utilise déjà `<details class="group">` pour « Voir les N autres avis », et il vient avant dans le DOM. Playwright a alors échoué sur « element is not visible » et j'ai failli croire à un bug de rendu | Ne jamais cibler un élément de test par une classe utilitaire Tailwind (`group`, `flex`…) : elles sont partagées. Partir de l'attribut de données propre au composant et remonter (`document.querySelector('[data-formulaire-avis]').closest('details')`)
[2026-07-29] | Pour inspecter un fichier .env sans révéler la clé, j'ai utilisé `sed 's/=.*/=<masque>/'` — mais l'utilisateur avait écrit « cle api emaillit : secret_... » sans signe `=`. Le masque n'a rien masqué et la clé s'est affichée en clair dans la conversation | Ne jamais masquer un secret par une règle qui SUPPOSE un format. Pour inspecter un fichier sensible : n'afficher que des méta-données calculées (longueur, empreinte sha256, nombre de lignes), jamais le contenu même partiellement transformé. Vérifier l'existence d'une variable avec `node -e "console.log(!!process.env.X, process.env.X?.length)"`
[2026-07-29] | L'utilisateur a créé son `.env` dans `public/.env`. Le `.gitignore` l'attrapait (motif `.env` sans slash = tous les niveaux), donc j'aurais pu croire le risque écarté — mais Astro COPIE `public/` dans le site publié : test fait, le fichier se retrouvait dans `dist/client/.env`, téléchargeable sur le web | Un secret dans `public/` d'un projet Astro/Vite est publié même s'il est ignoré par Git : ce sont deux mécanismes indépendants. Toujours tester par un build réel (`ls dist/client/.env`) plutôt que se fier au `.gitignore`. Et rappeler que le dossier `public/` est destiné au public — c'est son nom
[2026-07-29] | DEUX FOIS dans la même session, l'utilisateur a cru une fonctionnalité livrée alors que la Pull Request était seulement créée. La première fois il a fait tous les réglages Stripe puis constaté « je n'ai pas reçu l'email » — le code n'était pas sur master. La seconde fois il a écrit « tout fonctionne » en parlant de la PR précédente | Annoncer « PR créée » se lit comme « c'est livré ». Toujours écrire explicitement « RIEN N'EST EN LIGNE tant que la PR n'est pas fusionnée », et proposer la fusion dans le même message. Avant tout diagnostic de type « ça ne marche pas », vérifier D'ABORD que le code est sur la branche déployée : `git ls-tree origin/master --name-only <chemin>` coûte 2 secondes et évite de chercher dans les réglages du tiers
[2026-07-29] | L'API Emailit accepte `custom_fields` sur `POST /audiences/{id}/subscribers`, répond 201 Créé… et stocke un objet VIDE. Cinq formats testés (objet, tableau, `fields`, `attributes`, `metadata`), aucun endpoint de déclaration de champs (`/fields`, `/custom-fields` → 404). Sa doc publique ne mentionne nulle part cette limite | Une réponse 2xx ne prouve PAS que la donnée est enregistrée. Après le premier écrit sur une API tierce mal documentée, TOUJOURS relire l'objet créé et comparer champ par champ à ce qu'on a envoyé. Et faire ce contrôle sur UNE seule fiche avant d'en envoyer 1 550 — ici ça a évité un import complet à refaire
[2026-07-29] | La suppression d'un abonné Emailit (`DELETE /audiences/{id}/subscribers/{sub_id}`) renvoie 400 sans corps de requête : elle exige `{"email": "..."}` dans le corps, ce qu'aucune doc n'indique pour un DELETE | Quand un DELETE renvoie 400 alors que l'identifiant est bon, essayer d'ajouter le corps que l'API attendrait pour un POST équivalent. Nettoyer ses données de test AVANT de lancer un traitement de masse, sinon elles se noient dedans
[2026-07-29] | `check-redirects.mjs` annonçait « 53/53 OK » alors que les 6 URLs `/product-category/*/` du sitemap WordPress tombaient en 404 en production. Le joker Vercel `/product-category/:path*` ne capture PAS le slash final — et le script testait la règle avec un chemin d'exemple SANS slash (`sampleForWildcard`), donc il validait la seule forme qui marchait | Un test qui fabrique lui-même son cas d'exemple doit le fabriquer à l'image des VRAIES données, pas au plus simple : ici les URLs réelles à rediriger viennent du sitemap WP et ont toutes un slash final. Tester chaque règle dans ses DEUX formes (avec et sans slash). Un joker qui doit tout attraper s'écrit `(.*)`, pas `:path*`. Et un script de vérification au vert est une affirmation à auditer comme une autre : le confronter à un échantillon d'URLs réelles avant de s'y fier
[2026-07-30] | Après un clic sur « Refuser », le cookie `_gcl_au` posé lors d'une acceptation antérieure survivait : le traçage continuait donc APRÈS un refus. Le bandeau empêchait bien de poser de NOUVEAUX cookies, mais ne retirait rien. Trouvé uniquement parce que j'ai relu `document.cookie` après le refus sur le déploiement réel | Un retrait de consentement doit faire CESSER le traitement, pas seulement empêcher les suivants. Tester un bandeau, c'est tester le cycle COMPLET dans cet ordre : accepter → vérifier que les cookies sont là → retirer → vérifier qu'ils ont DISPARU. Tester seulement « refuser d'abord » ne voit jamais ce défaut, puisqu'il n'y a rien à effacer
[2026-07-30] | J'ai relancé `npm run build` alors que mon serveur de test tournait DEPUIS `dist/client` : échec sur `rmdirSync`, le dossier étant verrouillé par Windows. La leçon du 2026-07-20 disait déjà « lancer le serveur depuis la racine du projet » et je l'avais lue | Une leçon sur un piège d'environnement doit être appliquée au moment où l'on tape la commande, pas relue au moment où l'on débogue. Servir `dist/client` en passant le dossier en ARGUMENT (`python -m http.server -d dist/client`) au lieu de s'y déplacer : le processus ne verrouille alors plus le dossier que le build doit supprimer
[2026-07-30] | J'allais adopter l'API Fonts d'Astro (stable en 7.1.1) pour corriger le CLS : elle génère preloads ET replis à métriques ajustées, c'était la solution élégante. Son composant `Font.astro` émet `<style set:html={data.css}></style>` — du CSS INLINE, que la CSP `style-src 'self'` du projet bloque. Les @font-face ne se seraient jamais appliqués : le site aurait perdu ses polices, en silence côté build | Avant d'adopter une fonctionnalité de framework sur ce projet, LIRE ce qu'elle émet réellement dans le HTML (`cat node_modules/astro/components/X.astro`), pas seulement sa documentation. La CSP stricte (`style-src`/`script-src 'self'`, `inlineStylesheets: 'never'`) disqualifie tout ce qui injecte du style ou du script inline — c'est une contrainte structurelle du projet, pas un détail. La solution la plus élégante sur le papier peut être la seule inutilisable ici
[2026-07-30] | En passant les polices de `400.css` à `latin-400.css`, mon scan a trouvé 10 caractères hors du sous-ensemble latin (→ ★ ✓ emoji) et j'ai failli les traiter comme une régression à corriger | « Hors du périmètre couvert » n'est pas « cassé par mon changement ». Avant de conclure à une régression, mesurer l'état AVANT sur le même critère : ici les 188 plages `unicode-range` des fichiers complets ne couvraient AUCUN de ces caractères non plus, ils étaient déjà rendus par une police système — rendu identique. Et faire porter au script un contrôle de cohérence sur des cas dont on connaît la réponse (`é œ ® €` doivent ressortir couverts), sinon un scan qui ne trouve rien ne prouve rien
[2026-07-30] | J'ai voulu documenter une nouvelle redirection par un commentaire `//` dans `vercel.json`. Or `scripts/check-redirects.mjs` fait un `JSON.parse` du fichier : le commentaire aurait cassé le script de vérification des redirections, donc le filet de sécurité de la bascule | `vercel.json` est du JSON STRICT dans ce projet, pas du JSONC : aucun commentaire. Toute explication va dans `docs/` ou dans le message de commit. Avant d'ajouter un commentaire à un fichier de données, chercher qui le parse (`grep -rn "nom-du-fichier" scripts/ src/`) — un fichier de config peut être lu par un outil plus strict que la plateforme
[2026-07-30] | J'allais livrer `"trailingSlash": true` dans `vercel.json` pour supprimer les 404 sur les URLs sans slash final. Ce réglage est GLOBAL : il place un 308 devant toutes les routes, `/api/stripe-webhook` compris. Stripe ne suit pas les redirections et compte tout 3xx comme un échec de livraison — les emails de confirmation de commande auraient cassé, en boucle de rejeu | Avant d'activer un réglage de routage global (trailingSlash, cleanUrls, rewrite fourre-tout), énumérer TOUTES les routes qu'il traverse, et traiter à part celles appelées par une machine (webhooks, callbacks de paiement) : un client automatique ne se comporte pas comme un navigateur, il ne suit pas les redirections. Ici la solution sûre était 50 règles jumelles explicites, verbeuses mais sans effet de bord
[2026-07-30] | Mon propre script d'audit de couverture a déclaré 6 URLs « non couvertes » alors que la règle existait : sa fonction de compilation échappait `(.*)` comme du texte littéral au lieu de le traiter comme un joker. Un faux négatif, symétrique du faux vert de `check-redirects.mjs` que je venais de corriger | Un script qui compile des motifs (glob, regex, routes) doit être testé sur un cas dont on connaît la réponse AVANT de se fier à son verdict. Quand un résultat contredit une correction qu'on vient de vérifier par ailleurs, suspecter l'instrument de mesure avant la correction — et confronter les deux outils entre eux, c'est ce désaccord qui a révélé le bug
[2026-07-29] | J'ai mesuré le poids de l'accueil WordPress avec `curl -H "Accept-Encoding: br, gzip"` sans `--compressed` : curl a enregistré les octets COMPRESSÉS, et mon script a compté 0 script/0 image sur ce qu'il croyait être du HTML. J'ai failli conclure que la page WordPress ne chargeait aucun script | Pour analyser le CONTENU d'une page, utiliser `curl --compressed` (curl décompresse) ; pour mesurer le POIDS TRANSFÉRÉ, lire `%{size_download}`. Ne jamais poser `Accept-Encoding` à la main puis parser le corps. Signal d'alarme : un compteur à 0 sur une page dont on sait qu'elle est lourde n'est pas un résultat, c'est un bug de mesure
